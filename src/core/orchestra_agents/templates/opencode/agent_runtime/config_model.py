"""Temporary compatibility shim for Task 7.5; delete in Task 8."""

from __future__ import annotations

from core.orchestra_agents.backends.opencode.config_model import (
    _DEFAULT_MODEL as _DEFAULT_MODEL,
)
from core.orchestra_agents.backends.opencode.config_model import (
    _SCHEMA_URL as _SCHEMA_URL,
)
from core.orchestra_agents.backends.opencode.config_model import (
    build_root_payload as build_root_payload,
)
from core.orchestra_agents.backends.opencode.config_model import (
    resolve_model as resolve_model,
)
