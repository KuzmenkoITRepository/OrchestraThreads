"""Temporary compatibility shim for Task 7.5; delete in Task 8."""

from __future__ import annotations

from core.orchestra_agents.backends.opencode.backend_context import (
    clear_file as clear_file,
)
from core.orchestra_agents.backends.opencode.backend_context import (
    write_active_context as write_active_context,
)
