# DEPRECATED SERVICE

`src/core/llm_proxy/` is deprecated.

This service is scheduled for removal in the near term and should not be treated
as an active modernization target.

Rules for the current repository state:

- do not expand this service with new product logic;
- do not use it as the default target for new integrations;
- prefer migration and containment over deepening local complexity;
- expect this directory to be removed once the replacement path is ready.

This marker exists intentionally so the repository makes the service status
explicit.
